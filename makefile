git:
	git add .                 # adds every file in this directory to git
	git commit -m "$m"        # commits git to github with message '$m'
	git push -f origin main   # pushes the commit to the main branch